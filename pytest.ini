[pytest]
pythonpath = backend
testpaths = tests
# Default temp root lives under AppData on Windows and is often not writable by
# the test process; keep scratch inside the repo instead.
addopts = -q --tb=short --basetemp=.pytest_tmp
